ORG_ID=acme-corp
KAFKA_BROKER=localhost:9092
KAFKA_CLIENT_ID=sensor-simulator

# How often to reload sensors from Postgres (ms). New sensors appear within this window.
SENSOR_REFRESH_INTERVAL_MS=30000
# How often each sensor emits a reading (ms)
SENSOR_DATA_INTERVAL_MS=3000
