# Groq API (for LLM)
GROQ_API_KEY=

# Database (PostgreSQL + TimescaleDB, database name: pulseboard)
DB_HOST=localhost
DB_PORT=5433
DB_USER=postgres
DB_PASSWORD=admin
DB_NAME=pulseboard

# Service
PORT=5004
NODE_ENV=development
